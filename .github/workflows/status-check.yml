name: anyrouter-status-check

on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - status-check

concurrency:
  group: anyrouter-status-check
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  probe-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          npm install -g @anthropic-ai/claude-code@latest

      - name: Probe and persist data
        env:
          ANYROUTER_API_BASE: ${{ secrets.ANYROUTER_API_BASE }}
          ANYROUTER_API_KEY: ${{ secrets.ANYROUTER_API_KEY }}
          ANYROUTER_MODEL: ${{ secrets.ANYROUTER_MODEL }}
          ANYROUTER_CONSOLE_BASE: ${{ secrets.ANYROUTER_CONSOLE_BASE }}
          ANYROUTER_CONSOLE_SESSION: ${{ secrets.ANYROUTER_CONSOLE_SESSION }}
          ANYROUTER_CONSOLE_USER_ID: ${{ secrets.ANYROUTER_CONSOLE_USER_ID }}
          ANYROUTER_CONSOLE_QUOTA_PER_UNIT: ${{ secrets.ANYROUTER_CONSOLE_QUOTA_PER_UNIT }}
          ANYROUTER_CONSOLE_DISPLAY_IN_CURRENCY: ${{ secrets.ANYROUTER_CONSOLE_DISPLAY_IN_CURRENCY }}
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          attempt=1
          max_attempts=3

          while [ "$attempt" -le "$max_attempts" ]; do
            echo "Publish attempt $attempt/$max_attempts"

            git fetch origin main
            git reset --hard origin/main

            python scripts/check_anyrouter.py

            git add docs/data/status.json docs/data/history.json
            if git diff --cached --quiet; then
              echo "No data changes to commit"
              break
            fi

            git commit -m "chore(status): refresh anyrouter probe data"

            if git push origin HEAD:main; then
              echo "Publish succeeded"
              break
            fi

            echo "Push failed; retrying on top of the latest origin/main"
            attempt=$((attempt + 1))
          done

          if [ "$attempt" -gt "$max_attempts" ]; then
            echo "Failed to publish data after $max_attempts attempts" >&2
            exit 1
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
